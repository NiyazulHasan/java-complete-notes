Q) What is the difference between ROWID and ROWNUM ? 

ROWID 
-------
It is permanent.

It is a memory location where our records store in a table.

ex:
	select eid,ename,esal from emp;
	select rowid,eid,ename,esal from emp;	

ROWNUM 
-------
It is temperory.

It starts with 1 and increment by 1.

ex:
	select eid,ename,esal from emp;
	select rownum,eid,ename,esal from emp;
	select rowid,rownum,eid,ename,esal from emp;


Q) Write a query to display first three records from employee table?

	select * from emp where rownum between 1 and 3;
	or 
	select * from emp where rownum IN (1,2,3);
	or 
	select * from emp where rownum<=3;
	or
	SELECT * FROM emp ORDER BY eid 
			FETCH FIRST 3 ROWS ONLY;

	

Q) Write a query to display last three records from employee table?

	SELECT * FROM emp ORDER BY eid desc
			FETCH FIRST 3 ROWS ONLY;
	or 

	select * from (select * from emp order by eid desc) where rownum<=3;

Q) Write a query to read fourth record from employee table?

	select * from emp where rownum<=4
	minus 
	select * from emp where rownum<=3;

	or 

	SELECT * FROM emp ORDER BY eid
		OFFSET 3 ROWS FETCH NEXT 1 ROW ONLY;


Sequence 
=========
A sequence is an object which is used to generate the numbers.

syntax:
-------
	create sequence <sequence_name> start with value increment by value;
ex:
	create sequence sq1 start with 1 increment by 1;
	create sequence sq2 start with 101 increment by 1;
	create sequence sq3 start with 10 increment by 10;

We have two pseudo in a sequence.

1) NEXTVAL 
-----------
	It is used to generate next number in a sequence.
	ex:
	create sequence sq1 start with 101 increment by 1;
	drop table student;
	create table student(sno number(3),sname varchar2(10),sadd varchar2(12));
	insert into student values(sq1.NEXTVAL,'raja','hyd');	
	insert into student values(sq1.NEXTVAL,'ravi','delhi');	
	insert into student values(sq1.NEXTVAL,'ramana','vizag');
	commit;			

2) CURRVAL 
----------
	It returns last number generated by the sequence.
	ex:
		select sq1.CURRVAL from dual;


Q) Write a query to display list of sequences present in a database table?

	select sequence_name from user_sequences;


Q) Write a query to drop the sequence?

	drop sequence sq1;


TCL Commands 
============
We have three TCL commands.

1) commit 

2) rollback 

3) savepoint 

1) commit
-----------
It is used to make the changes permanent to database.

ex:
	drop table student;
	create table student(sno number(3),sname varchar2(10),sadd varchar2(12));
	insert into student values(101,'raja','hyd');
	insert into student values(102,'ravi','delhi');
	commit;
	select * from student; // 2 records selected 


2) rollback 
-----------
It is used to undo the changes which are not permanent.
ex:
	drop table student;
	create table student(sno number(3),sname varchar2(10),sadd varchar2(12));
	insert into student values(101,'raja','hyd');
	insert into student values(102,'ravi','delhi');
	select * from student; // 2 rows selected 
	rollback;
	select * from student; // no rows selected 

3) savepoint
-------------
It is a marker in active transaction to perform partial rollbacks.

Instead of complete rollback we can rollback upto save point.

ex:
	drop table student;

	create table student(sno number(3),sname varchar2(10),sadd varchar2(12));

	insert into student values(101,'raja','hyd');
	insert into student values(102,'ravi','delhi');

	savepoint sp1;

	insert into student values(103,'ramana','vizag');
	insert into student values(104,'ramulu','pune');

	savepoint sp2;

	insert into student values(105,'Jack','Texas');
	insert into student values(106,'John','Chicago');

	select * from student; // 6 rows selected 

	rollback to sp2;

	select * from student; // 4 rows selected 


DCL commands 
============

Schema: 
-------
	It is a memory location where SQL commands execute.

Privileges:
-----------
	Permissions given to a user is called privileges.
	Rights given to a user is called privileges.
	We have two types of privileges.
	1) System privilege : Permissions given by DBA to user.
	2) Object privilege : permissions given by one user to another user. 

grant 
-----
It is used to grant the permissions to the user.

syntax:
------
	grant <privilege1>,<privilege2> to <user_name>;

revoke 
------
It is used to revoke the permissions from the user.

syntax:
------
	revoke <privilege1>,<privilege2> from <user_name>;	

DBA> create user kranti identified by kranti123;
DBA> create user jotika identified by jotika123;

kranti> conn kranti/kranti123  -- logon denied 
jotika> conn jotika/jotika123  -- logon denied 

DBA> grant connect,resource to kranti,jotika;

kranti> conn kranti/kranti123  
jotika> conn jotika/jotika123


kranti> 

create table employee(eid number(3),ename varchar2(10),esal number(8));
insert into employee values(201,'Alan',10000);
insert into employee values(202,'Jose',20000);
insert into employee values(203,'Nelson',30000);
commit;
select * from employee;


jotika> select * from employee; --table or view does not exist 

kranti> grant select on employee to jotika;

jotika> select * from kranti.employee;

jotika> delete from kranti.employee; -- insufficient privileges

kranti> grant delete on employee to jotika;

jotika> delete from kranti.employee;
jotika> commit; 

kranti> revoke select, delete on employee from jotika;

kranti> disc 
jotika> disc 

DBA> revoke connect,resource from kranti,jotika;





